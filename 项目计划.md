# V2V 实时语音翻译 — 项目计划

> 本文档是范围锁定后的执行主线。`README.md` 是总览，`vox.md` 是当前执行范围，本文是落地计划。
> 建立日期：2026-08-22

---

## 一、项目定位（已锁定）

- **做什么**：消费级、实时**中英**语音翻译，**端到端**模型，不考虑本地部署。
- **首批场景**：**游戏**优先（数据最好拿），聚焦 **多人 FPS**（Valorant / CS）作为试点。
- **目标用户场景**：游戏直播 / 社交 / 直播间，年轻人群体。
- **不做（明确排除）**：医疗/法律/金融等专业领域、纯企业级、文字图像联合理解、**追求完美中英逐字配对数据**（现实拳不到，已放弃，见训练方法）。

## 二、技术路线（已锁定，2026-08-24 定，08-29 修）

> ⚠️ 本节推翻旧版"Phase1 只做 S2TT、Phase2 才 S2ST + TTS 造伪对"的路线。
> 目标形态：**多模态端到端**（音/字 输入 × 音/字 输出 四象限都要），**不造声**（不 TTS 造对），**不从头训**，微调现成双端端到端底座。

| 层面 | 选择 | 说明 |
|------|------|------|
| **部署形态** | 多模态端到端（音/字 输入 → 音/字 输出） | 四象限：音→字、字→字、字→音、音→音。**不**走部署→级联 |
| **训练方法** | 微调现成双端端到端底座 | 选有"听+说"能力的开源双模态基座（如 Seamless 系 / 开源 ASR+TTS），**微调域适配到 FPS 黑话窄域**，不从零训 |
| **训练监督** | **直接拿视频音频训**（纯英 + 纯中） | **主力 = 视频音频本身**。不去追完美中英配对（现实"近乎没真实双语音对"）；LLM 只打辅助（见下），不决定训练主逻辑 |
| **数据来源** | **不配"对随机凑**，直接训 | 中文=B站/抖音开黑；英文=YouTube/Twitch开黑。音频在分类时打类型标签（你人工+LLM辅助），不分抽"中英夹杂"当数据源 |

> **关键认知（2026-08-29 修正，推翻"LLM 做中英语义对账"）**：
> - **训练主力 = 纯英 + 纯中的真实开黑视频音频**，直接喂底座微调；完美中英句级配对本就不存在，明确放弃。
> - **LLM 只做辅助**（不全权）：采集层自动打"游戏/内容类型"标签；生成难例/伪负例助入；做生成结果的評価。**不承担"造配对监督"职责**。
> - **标签最终你人工把关**：你在部分 LLM 标完成后再核一遍，或你直接给主标签、LLM 补缺。
> - **说话人分离不做**：FPS 多人重叠、"分谁说话"没意义，按**段级战术意群块**（多人重叠一起进块），不做 VAD 逐句、不做 speaker diarization。
> - 端到端音→音的现实**风险是可能退化为"两级 + TTS"**，按用户立场不因此放弃端到端；达成路径待底座调研后定（见第六节开放问题）。

## 三、数据方案

**来源（指向"玩家语音"，不是解说/播报）**
- 英文：**Twitch / YouTube Gaming 的点播视频/VOD/回放**（不做实时录直播）
- 中文：**抖音 / B站 / 快手等平台的点播游戏视频**（含直播录屏再发布的集锦 —— 直播本身无回放，但有人录屏再发，人工找账号即可）
- **采集形态**：**只抓点播/回放，不实时录直播**（用户人工跑账号、筛热门排序）
- **筛选硬约束**：必须**多人游戏** + **真人对话**（队友开黑/语音），按游戏分类 + 热门排序取。
- **分类在采集时就判断（NEW）**：按标题/时长/类型在采集层直接分类，**不分析音频**；中文侧与英文侧套**同一套分类规则**，别等爬到一半再分。
- **明确排除**：电竞解说 / 赛事播报 —— 它是**单人职业化播报、非玩家语音**，不符合"多人同时说话 / 嘲讽隐喻文化语境 / 真人对话"，对目标场景无用。

**每侧数据（音 + 字幕）**
- 每侧抓「真人原声 + 字幕」：字幕来源 = 平台字幕 + CPU ASR（已验证 FunASR / SenseVoice，CPU 28x）转**段级字幕**。
- 字幕双用途：a) 音/字口的训练/监督数据；b) 给 LLM 辅助打标 / 做评估的输入。
- 音频在采集层**只存原始**、不做调音；降噪/人声增强放到 **M2 清洗层**（highpass + dynaudnorm，已验证）。

**标签（你 + LLM 辅助）**
- 分类/标注 = **你人工把关，LLM 辅助**：用视频标题/时长/ASR 字幕文本，LLM 预打"游戏/内容类型"标签 → 你抽查复核。不追求完美双语，只为训练数据分好类、可检索、可筛。

**语义对齐（可选辅助，非主线）**
- 若确有真实同源中英字幕/配音（如游戏多语言官播），可让 LLM 做一次**传意对齐**产出可选台账，作为辅助增强；**不当作数据瓶颈、不阻塞主训练。** 缺则正常训纯英+纯中。

**专有名词**：跟着游戏类别走，首批建 **FPS 词汇表**（Valorant/CS 的地图、技能、武器、报点、战术黑话、嘲讽/社交黑话）。

## 三-A、基础设施与代码方式

- **服务器**：UpCloud 美国圣何塞节点（US-SJO1），4核/8GB/800GB，Ubuntu，SSH 密钥登录。
  - 在美国 → **访问 Twitch/YouTube 原生无障碍**（英文侧不需要代理）。
  - 访问中文平台（抖音/B站）需走代理回国内（已有 clash 配置 + 新加坡节点可用）。
- **代码方式**：本机只做设计 + 选型，**不从零写生产爬虫**；优先采用**开源爬虫项目**改造。代码最终在服务器跑大批量。
- **落地顺序**：**英文侧与中文侧并行推进**（英文侧服务器原生、工具成熟；中文侧需过代理、反爬更硬，但**纯中数据也是训练所必需**，必须尽早启动，避免只攒单语）。当前优先开英文侧持续放量 + 中文侧 B站 启动采集。
- **英文侧内部顺序**：**先 YouTube 打头**（理由见下"英文侧平台对比"），Twitch 随后。

## 三-B、英文侧平台对比（决定先做 YouTube）

| 维度 | YouTube 点播 | Twitch 开黑直播 |
|------|------------|--------------|
| **字幕** | ✅ **平台自带**（人工+自动），直接拿来用，省掉 ASR | ❌ 无字幕系统，每条都要自己跑 ASR |
| **存档** | ✅ 永久存在，随时可重抓 | ⚠️ **默认14天自动删**；大量开黑主播根本不存 VOD；且 Twitch 缺成熟的第三方录屏发布生态 |
| **玩家语音原生态** | 一般（多为剪辑/上传，可能有配音处理） | ✅ 更原生态（队友开黑实况） |
| **结论** | **天然主力**，永久存档+自带字幕≈零摩擦 | 语音更原生态，但无字幕+VOD易失+要自跑ASR，成本高一截 |

> ⚠️ Twitch 的"开黑直播不留 VOD / 14天删除"与"只抓点播、不实时录"决定正面冲突。Twitch 若要做，需推翻"只抓点播"→ 改为盯主播实时录制。故 Twitch 后置。

## 三-C、M1 工具选型（已敲定）

**YouTube（主力）**
- 发现：**yt-dlp 吃带 `sp=` 排序参数的搜索 URL**（零配置，`--flat-playlist` 先出列表筛选）；精确排序可选 YouTube Data API v3（注意 search.list 每天限100次）
- 下载：**yt-dlp**（唯一靠谱，最新版 2026.08.19，极其活跃；youtube-dl 已停更5年，禁用）
- 字幕：**yt-dlp 自带** `--write-subs --write-auto-subs`（人工优先、自动兜底）
- 排除直播：`--match-filter "!is_live & !live & !is_upcoming"`
- **必须保持最新**（`yt-dlp -U`）+ 挂 cookies，对抗反爬

**Twitch（后置，作为后续可选）**
- 发现：**Twitch Helix API** `GET /videos`（`game_id` + `sort=views` + `type=archive` + `language=en`），用官方 `twitch-cli` 或 Python `TwitchIO`
- 下载：**twitch-dl**（`-q audio_only` 专为音频，多线程）+ yt-dlp 兜底
- 长期归档（防14天删除）：可选 ganymede / vod2pod-rss

## 四、Phase 1 目标：中等规模出 baseline

完成标准：**Valorant 单游戏，数千小时级数据，出一个可评估的 S2TT（字幕）baseline**。

| 里程碑 | 内容 | 主要困难 |
|--------|------|---------|
| **M1 爬虫管线** | 单游戏(Valorant)跑通：抓取→下载→存储。**英中双语并行**（英文 YouTube 已跑通；中文 B站 启动）。分类在采集时判断。 | 反爬、流媒体解锁、ToS、代理(中文侧)、存储 |
| **M2 字幕提取清洗** | 音视频分离、段级字幕提取(ASR)、降噪/人声增强、按游戏/热门分类；**不做 VAD 逐句、不做说话人分离** | 字幕质量参差、玩家语音混在 BGM/音效里、重叠段处理 |
| **M3 打标与数据整理** | 用 LLM **辅助**打"游戏/内容类型"标签（你人工抽查复核）；可选：若有真实同源中英字幕则做传意对齐增强（非主线）。缺则直接进 M4 训纯英+纯中 | 标签质量、LLM 打辅助的把握、token 成本 |
| **M4 baseline 训练** | **微调现成双端端到端底座**（不从零训），**直接喂纯英+纯中的开黑视频音频**分段训练，目标=学到 FPS 黑话窄域（报点/嘲讽/混音造腔）。难点：算力(GPU)、底座选型、端到端音/音可达性（风险：可能退化两级+TTS，见第二节） |

> 爬虫是 M1 也是整个 Phase 1 的瓶颈，**游戏分类 / 热门排序 / 多人游戏筛选**都在 M1 落地。

## 五、主要风险

1. **爬虫（最大）**：抖音/B站反爬、Twitch 访问、版权/ToS 合规、存储成本。
2. **数据质量与打标（M3 难点）**：抓来的开黑音频噪音大、重度重叠、中英夹杂；垃圾分类/标注质量直接影响训练，需你人工把关+LLM辅助。
3. **玩家语音获取**：直播音轨里玩家语音质量参差（背景音/BGM/噪声）、**多人重叠**；已定不做说话人分离，重叠段按战术意群块处理。
4. **端到端音→音可达性**：直接喂纯英+纯中音频微调能否真正训出端到端，还是退化为两级+TTS（见第二节，待底座调研）。
5. **合规**：直播内容版权与平台 ToS，存储与再分发需谨慎界定（仅训练用）。

## 六、开放问题（待定，不阻塞起步）

- [ ] **产品形态**：字幕 overlay？语音转换后端输出？直播间插件？API？（影响 M4 输出设计）
- [ ] **存储与算力预算**：数千小时音视频的存储位置、GPU 来源。
- [ ] **抖音/B站爬虫具体方案**：是否走直播流录制、代理池、B站反爬策略。
- [ ] **标签打法 + LLM 辅助边界**：LLM 打标签/难例能帮到什么程度、你抽查的比例；混音/新词学风怎么衡。
- [ ] **底座选型（M4）**：选哪个现成双端端到端开源模型做微调（Seamless 系 / 开源 ASR+TTS 底座），需调研；**优先选原生能做音→音的，省去手工配对**。
- [ ] **评估方法（M4）**：域内"听懂能回嘴"怎么定量评估（尚无方案）。
- [ ] ~~TTS 选型~~（已废弃：整体上已不造声；TTS 仅可为可选工具，非唯一配对方式）。
