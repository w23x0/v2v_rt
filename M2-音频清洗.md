# M2 - 音频清洗流水线

> 阶段：Phase 1 / 里程碑 M2 的第一步——**音频预清洗**（VAD/ASR 的上游预处理）。
> 执行位置：UpCloud 美西服务器。建立日期：2026-08-23

---

## 一、设计原则

清洗分成两层，本文件是**第一层（纯 ffmpeg 预清洗）**，第二层（VAD 切分）待 ASR 选型后接入。

**为什么要自跑 ASR（用户的决定）**：不用 YouTube 自动字幕（碎片多、质量差），改在 CPU 上自己跑 ASR 模型转文字。因此"清洗"的对象从"字幕文本"变成"音频本身"——这是标准 DSP 活，更可控。YouTube 字幕保留作参照基线 + 兜底。

## 二、预清洗规格（已落地）

| 项目 | 原始 | 清洗后 |
|------|------|--------|
| 采样率 | 48kHz 立体声 | **16kHz 单声道**（ASR 标准）|
| 滤镜 | — | highpass 80Hz（去低频嗡嗡）+ dynaudnorm（动态音量归一）|
| 体积 | 3.2 GB | 539 MB（降 83%）|
| 时长 | 保持不变 | 保持不变 |
| 处理速度 | — | 7 个文件 / 13 秒（~1500x 实时）|

产出在 `~/v2v-data/{game}-cleaned/`，原始文件不动。

## 三、脚本

服务器上 `~/v2v-scripts/preprocess_audio.sh`，用法：
```bash
~/v2v-scripts/preprocess_audio.sh valorant     # 清洗 valorant 目录
```

滤镜链（均为轻量单遍，不卡长音频）：
```
highpass=f=80, dynaudnorm=p=0.9, aresample=16000, -ac 1
```

## 四、踩坑记录（重要，别重犯）

1. **`loudnorm` 对长音频会卡死**：动态模式反复缓冲分析，2520s 文件跑 26 分钟没完成。
   → 换 `dynaudnorm`（快约 1000 倍，效果对起步够用）。音量精确归一不是 ASR 的硬需求。
2. **`while read -d ""` 循环 bug**：在 `set -e` + 子shell 下，偶数序号文件路径被污染导致 FAILED。
   → 改用 `mapfile -d '' -t FILES < <(find ... -print0)` 数组遍历。
3. **文件名带空格**：`for f in $(find ...)` 会按空格切碎路径。
   → 一律用 `find -print0 | while IFS= read -r -d "" f` 或 `mapfile -d ''`。

## 五、下一步（待 ASR 选型后）

1. **VAD 切分**：用 silero-vad / FSMN-VAD 把清洗后的音频切成"有人说话"的短段，砍掉静音/纯枪声段（省 ASR 算力，游戏音频一半时间没人说话）。
2. **ASR 转写**：CPU 跑选定模型，`音频段 → (时间戳 + 文字)`。
3. **对标 YouTube 字幕**：用已有字幕评估 ASR 准确率，必要时字幕兜底。
