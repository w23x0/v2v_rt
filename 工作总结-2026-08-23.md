# 2026-08-23 工作总结

> ⚠️ 部分内容已过期（2026-08-24）。**第1节「起步=音→字→字配对、TTS做音对留Phase2」已被新路线推翻**：改为「多模态端到端 + 不造声 + LLM文字层对齐 + 微调现成底座」。保留本文作当日验证结果存档。

---

## 今日达成的里程碑

**Phase 1 前半段管线全部跑通并验证**：

```
YouTube 开黑视频 → yt-dlp 采集 → ffmpeg 预清洗 → FunASR+SenseVoice → 文字
```

7 个 Valorant 职业战队队内语音视频（4.9 小时）成功转成干净文字。

---

## 1. 范围收敛（多轮追问后锁定）

经多轮决策对话，从最初模糊的"实时语音翻译"收敛为清晰边界：

- **做**：消费级、端到端、中英双语、游戏优先、首批 FPS（Valorant/CS）
- **不做**：中英夹杂、专业领域、纯企业级；**排除电竞解说**（单人播报非玩家语音）
- **架构**：纯端到端 音→音；**拒绝**部署期 STT→MT→TTS 串联
- **起步**：音→字→字配对训练（CPU 可跑），TTS 造音对留到 Phase 2
- **采集**：只抓点播/回放，不实时录直播；先英文 YouTube，Twitch 后置，中文最后

**关键技术澄清**（纠正了几个错误认知）：
- 单语言玩家语音**不是没有配对**——音→字→字配对完全成立，文字配对不缺
- 缺的只是音↔音平行对，那是 Phase 2 (TTS) 的事，Phase 1 不碰

## 2. 基础设施

- **服务器**：UpCloud 美西 US-SJO1（4核/8GB/800GB，Ubuntu 26.04，原生访问 YouTube）
- **连接**：`ssh -i ~/.ssh/id_ed25519 w23x@209.50.60.104`
- **中文侧**：已有 clash 代理 + 新加坡节点（回国内访问抖音/B站，待用）
- **代码方式**：不从零写爬虫，用开源项目（yt-dlp / FunASR）改造

## 3. 工具选型（均经实时验证活跃度）

| 环节 | 选型 | 关键依赖 |
|------|------|---------|
| 采集 | yt-dlp 2026.08.19 | 🔑 deno（JS运行时）+ curl_cffi（反爬伪装），不加会失败 |
| 预清洗 | ffmpeg（highpass + dynaudnorm）| 避开 loudnorm（长音频卡死）|
| ASR | FunASR + **SenseVoice-Small** + FSMN-VAD | CPU 28x实时，中文优、嘈杂鲁棒 |

排除项：youtube-dl（停更）、Whisper 系（嘈杂幻听）、moonshine/distil（仅英文）、DeepFilterNet（停更）。

## 4. 踩坑记录（都已修复）

1. **yt-dlp 无 JS 运行时**：新版解析 YouTube 签名要 JS 运行时 → 装 deno + `--js-runtimes deno`
2. **yt-dlp 无反爬伪装**：YouTube 浏览器指纹检测 → `pipx inject yt-dlp curl_cffi`
3. **loudnorm 对长音频卡死**：26分钟没完成一个文件 → 换 dynaudnorm，快约1000倍
4. **`while read -d ""` 循环 bug**：偶数序号文件路径被污染 → 改 `mapfile -d ''` 数组遍历
5. **文件名带空格**：`for f in $(find)` 切碎路径 → 一律用 `find -print0 | while read -d ""`
6. **Python 3.14 PEP668**：pip 直接装被拦 → 用 pipx（yt-dlp）+ venv（funasr）

## 5. 性能基线（CPU 4核，实测）

| 任务 | 数据 | 耗时 | 吞吐 |
|------|------|------|------|
| 预清洗 | 4.9h 音频 | 13s | ~1500x 实时 |
| ASR 转写 | 2min 样本 | 4.3s | **28x 实时** |
| ASR 内存峰值 | — | 3.5GB | （8GB 中，富余）|
| ASR CPU 利用 | — | 156% | （4核=400%，**未榨干**）|

**结论：瓶颈是 CPU 不是内存**。内存富余巨大；CPU 只用满 1.5 核，调线程数还有加速空间。

## 6. 关键策略洞察

- **搜索命门词 `comms`**：电竞圈指"队内语音"，主播常把开黑语音录进视频。搜 FPS 语料必须带 `comms`/`team voice`，否则搜到无语音的纯画面录像
- **SenseVoice 的核心价值**：内置音频事件检测，把 BGM/枪声**标成事件标签**而非幻听成乱码——正好解决游戏音频痛点
- **YouTube 字幕的新角色**：从"主要文字来源"降级为 ASR 准确率的**参照基线**（算 WER）+ 信心低时兜底

---

## 当前资产

**服务器**：`~/v2v-scripts/`（youtube_collect.sh + preprocess_audio.sh）+ `~/venvs/funasr/` + 模型缓存
**数据**：`~/v2v-data/valorant`（原始 3.2GB）+ `valorant-cleaned`（539MB）
**文档**：项目计划 / M1采集 / M2清洗 / ASR选型 四份
**记忆**：8 条长期记忆（范围/路线/基础设施/工具/关键词/清洗/ASR/偏好）

## 下次接着做的点

1. 正式转写脚本：遍历 cleaned 目录全量转写（预计 10 分钟转完 4.9 小时）
2. 准确率评估：对标 YouTube 字幕算 WER
3. 放量采集：跑更多搜索词攒大批数据
4. 中文侧：启动抖音/B站点播采集（走代理）
5. M3：LLM 翻译配对中英文字（需先有中文数据）
