# 数据范围与分类标准

> 本文把产品范围、Phase 1 数据范围和采集判定分开。它是采集、清洗、标注和调度的共同依据。
> 建立日期：2026-08-31。

## 1. 产品范围

V2V 是面向消费级用户的**实时中英语音翻译**产品，目标使用场景是游戏、社交媒体和直播间。

- 交互形态：多模态端到端，支持音频/文字输入与音频/文字输出四种组合。
- 首个验证场景：多人在线游戏，优先多人 FPS。
- 目标特征：多人同时说话、低停顿、游戏专有名词、嘲讽和隐喻等文化语境，以及低延迟。
- 当前不做：医疗、法律、金融等专业领域；纯企业级场景；文字图像联合理解；本地部署。

产品范围不是“只做 Valorant”。Valorant/CS 是 Phase 1 的验证入口，后续可扩展到其他游戏和社交场景。

## 2. Phase 1 数据范围

Phase 1 的任务是为 FPS 窄域做出可评估的基线。当前采集白名单如下：

- 游戏：Valorant、CS/CS2；先以 Valorant 验证管线，CS/CS2 随后接入。
- 内容：**普通玩家**的多人对局、组队语音和排位回放为主；scrim/custom 或职业玩家的非赛事原声只能作为低优先级补充。
- 声音：玩家真实队内语音或多人实时交流，允许重叠、枪声、脚步声和自然背景噪声。
- 载体：YouTube 等平台的点播/VOD/回放；当前不把实时直播录制作为 M1 的默认来源。
- 语言：中文、英文以及真实混合语音都可以进入采集候选；后续在 ASR/数据整理阶段按语言侧和片段质量处理。
- 字幕：平台字幕不是硬门槛；有字幕则保存作为 ASR 参照，没有字幕也可以用自跑 ASR 生成段级文本。

### 2.1 采集时的三态判定

采集层只依据标题、描述、时长、频道和可见元数据做初筛，不假装能从元数据确定音频内容。

| 状态 | 判定 |
| --- | --- |
| `accept` | 明确是多人对局/回放，且标题或频道信息显示有玩家语音；通常为 20 分钟以上的长视频 |
| `review` | 游戏和多人对局明确，但是否有队内语音、是否完整或内容类型不确定；先入低优先级队列，人工抽查 |
| `reject` | 命中排除类型，或明确不是多人真实交流 |

时长是筛选信号而不是内容定义：20 分钟以下通常拒收；20--30 分钟进入复核；完整多局视频通常在 30 分钟以上，但不把 30 分钟写成绝对门槛。

## 3. 视频类型分类

每个候选视频至少记录以下字段。字段值可以是 `unknown`，不能为了填满标签而猜测。

| 维度 | 推荐值 |
| --- | --- |
| `game` | `valorant` / `cs2` / `other` / `unknown` |
| `platform` | `youtube` / `twitch_vod` / `bilibili` / `douyin` / `other` |
| `format` | `full_match` / `ranked` / `scrim` / `custom` / `highlight` / `tutorial` / `broadcast` / `other` |
| `voice_source` | `team_comms` / `multi_player_chat` / `solo_commentary` / `caster_broadcast` / `no_commentary` / `synthetic` / `unknown` |
| `player_type` | `ordinary_player` / `professional_player` / `official_broadcast` / `unknown` |
| `language` | `en` / `zh` / `mixed` / `other` / `unknown` |
| `speech_confidence` | `high` / `medium` / `low` |
| `quality_flags` | `bgm` / `heavy_noise` / `overlap` / `silence` / `none` |
| `subtitle_source` | `human` / `auto` / `asr` / `none` |

其中 `voice_source` 和 `format` 是采集决策的核心；`language`、`quality_flags` 和 `subtitle_source` 主要在下载/ASR 后补齐。

## 4. 明确排除

以下内容不进入 Phase 1 训练候选：

- 电竞赛事官方转播、解说或分析节目（`caster_broadcast`，不是玩家队内交流）。
- 赛事录像、联赛、季后赛、官方比赛和以职业赛事为中心的内容；它们不是普通玩家语料。
- 高光、剪辑、搞笑合集、montage、shorts 等后期拼接内容（除非人工确认其中包含完整且可独立使用的原始对局段）。
- 教学、攻略、reaction、地图讲解等以单人讲解为主的内容。
- `no commentary`、纯游戏画面、纯 BGM、纯音效或明确没有人类语音的内容。
- TTS、配音、合成旁白或其他非自然玩家对话。
- 非白名单游戏，除非作为后续扩展实验单独建批次。

## 5. 下载后复核与训练筛选

下载后用短音频抽查和 ASR 结果复核采集判定：

1. 确认是否真的存在多人普通玩家语音；若只有单人解说或赛事播报，改为 `reject`。
2. 保留真实重叠和游戏噪声；只在整段无人声、纯音乐或语音完全不可懂时丢弃片段。
3. `mixed` 不在采集阶段一律丢弃。后续根据片段语言和训练任务，将片段归入英文侧、中文侧、混合待处理或丢弃。
4. 记录人工复核结果和原因，供下一轮调度调整搜索词、频道白名单和黑名单。

## 6. 调度使用方式

调度器每轮应按以下顺序工作：

1. 读取本文件规定的白名单、排除项和历史结果。
2. 从尚未充分覆盖的 `game × format × language` 组合选择下一批搜索词。
3. 先 flat 探测并按元数据做 `accept/review/reject`，再下载 `accept` 和抽样的 `review`。
4. 下载完成后触发清洗和 ASR，写回标签、质量标记和失败原因。
5. 根据各类别的接受率、重复率和语音复核结果更新下一轮候选；不让模型凭空扩大范围。

调度器可以由 Claude 提供每轮的搜索词建议，但持久运行、断点恢复、重试和进程监督必须由普通脚本或 systemd/tmux 负责。

## 7. 去重设计

去重分层处理，不用标题相似度代替唯一身份：

1. **探测批次去重**：按 `platform + video_id` 去重；缺少 ID 时使用去掉查询参数和末尾斜杠的规范化 URL。`dedupe.py` 和分类器负责这一层。
2. **下载去重**：每个平台/游戏保留 yt-dlp `--download-archive`（现有英文侧为 `done.txt`），跨搜索词、跨轮次阻止同一视频再次下载。只有下载成功后才写入 archive。
3. **文件去重**：下载后记录原始媒体和清洗后音频的 SHA-256。相同文件可以直接跳过；同一视频不同格式的文件不能只靠文件名判断。
4. **近重复处理**：不同 ID 的搬运视频先标记 `possible_duplicate`，依据频道、归一化标题、时长和可选音频指纹进入人工复核，不能仅凭标题自动删除。
5. **转写去重**：转写记录的主键是 `platform + video_id + audio_variant + segment_start + segment_end`。不能因为文本相同就全局删除，因为游戏里的常见报点词本来会反复出现。

每条记录至少保留 `source_url`、`platform`、`video_id`、`downloaded_at`、`sha256`、`status` 和 `reject_reason`，这样规则调整后仍能追溯“为什么没收”。
